<input type="hidden" name="order_id" id="order_id" value="{{ order_id }}">
<input type="hidden" name="is_finalized" id="is_finalized" value="{{ is_finalized }}">
<h3><img src="{{ header_icon }}">&nbsp;O valor pago &eacute; maior que a fatura!</h3>

Foi realizado o pagamento {{ overpaid_amount }} {{ payment_currency }} a mais que o necess&aacute;rio!

<div id="tx_info" style="display: block; padding: 15px; margin-bottom: 20px; border: 1px solid transparent; border-radius: 4px; box-sizing: border-box; background-color: #dff0d8; border-color: #d6e9c6; color: #3c763d;">
  Os seguintes IDs de transa&ccedil;&atilde;o foram usados para pagar essa fatura:
  <ul id="txlist" style="padding-left: 20px;">
    {% for txid in txid_list %}
      <li>{{ txid }}</li>
    {% endfor %}
  </ul>
</div>

Os reembolsos podem ser dados de acordo com a pol&iacute;tica do propriet&aacute;rio da loja sobre pagamentos em excesso. Entre em contato com o propriet&aacute;rio da loja sobre a liquida&ccedil;&atilde;o do saldo do pagamento em excesso.

Obrigado por escolher pagar com {{payment_currency}}!

Veja os detalhes do pedido abaixo.
